import { AdminPopupResponsesClient } from "@/components/route-clients";

export const metadata = {
  title: "Pop-up Responses | Admin",
};

export default function Page() {
  return <AdminPopupResponsesClient />;
}
